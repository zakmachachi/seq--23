; PlatformIO Project Configuration File
;
;   Build options: build flags, source filter
;   Upload options: custom upload port, speed and extra flags
;   Library options: dependencies, extra library storages
;   Advanced options: extra scripting
;
; Please visit documentation for the other options and examples
; https://docs.platformio.org/page/projectconf.html

[platformio]
; Load any local *_override.ini if present (e.g. platformio_override.ini for an
; upside-down PCB: -D SCREEN_ROTATION=2). The glob is silently ignored when no
; override file exists, so the public release builds at the 0° default.
extra_configs = *_override.ini

[env:teensy41]
platform = teensy
board = teensy41
framework = arduino
lib_deps =
  adafruit/Adafruit GFX Library
  adafruit/Adafruit SH110X
  Debounce
  adafruit/Adafruit NeoPixel